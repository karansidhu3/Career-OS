[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
pythonpath = .
addopts = -v --tb=short
markers =
    integration: requires TEST_DATABASE_URL (real Postgres)
